*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing — never commit the keystore or its passwords
keystore.properties
*.keystore
*.jks

# Built APKs go to GitHub Releases, not the repo
*.apk

# Scratch / working files (source art before it's resized into res or assets)
/tmp

# Claude Code worktrees / session scaffolding
/.claude/

# Local development notes/tooling — keep out of the public repo
/CLAUDE.md
/tools/
/docs/GECKOVIEW_PORT_PLAN.md
